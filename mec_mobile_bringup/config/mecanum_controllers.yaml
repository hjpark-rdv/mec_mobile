controller_manager:
  ros__parameters:
    update_rate: 100

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    mecanum_drive_controller:
      type: mecanum_drive_controller/MecanumDriveController

mecanum_drive_controller:
  ros__parameters:
    # ↓↓↓ 여기를 여러분 URDF의 실제 조인트명으로 정확히 바꾸세요 ↓↓↓
    front_left_wheel_command_joint_name:  "front_left_wheel_joint"
    front_right_wheel_command_joint_name: "front_right_wheel_joint"
    back_left_wheel_command_joint_name:   "back_left_wheel_joint"
    back_right_wheel_command_joint_name:  "back_right_wheel_joint"

    kinematics:
      wheels_radius: 0.05
      # 로봇 중심에서 각 휠까지의 x, y 투영 거리의 합 (lx + ly)
      sum_of_robot_center_projection_on_X_Y_axis: 0.25

      base_frame_offset: {x: 0.0, y: 0.0, theta: 0.0}

    use_stamped_vel: false
    base_frame_id: "base_link"
    odom_frame_id: "odom"
    enable_odom_tf: true
    pose_covariance_diagonal:  [0.001, 0.001, 0.0, 0.0, 0.0, 0.001]
    twist_covariance_diagonal: [0.001, 0.001, 0.0, 0.0, 0.0, 0.001]
